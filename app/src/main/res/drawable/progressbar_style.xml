<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="30dp" />
            <gradient
                android:angle="270"
                android:centerColor="@color/progressbar_first_color"
                android:centerY="0.75"
                android:endColor="@color/progressbar_first_color"
                android:startColor="@color/progressbar_first_color" />
        </shape>
    </item>

    <item android:id="@android:id/secondaryProgress">
        <clip>
            <shape>
                <corners android:radius="30dp" />
                <gradient
                    android:angle="270"
                    android:centerColor="@color/progressbar_second_color"
                    android:centerY="0.75"
                    android:endColor="@color/progressbar_second_color"
                    android:startColor="@color/progressbar_second_color" />
            </shape>
        </clip>
    </item>
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="30dp" />
                <gradient
                    android:angle="270"
                    android:endColor="@color/progressbar_second_color"
                    android:startColor="@color/progressbar_second_color" />
            </shape>
        </clip>
    </item>

</layer-list>